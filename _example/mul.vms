label begin
    push 25
    push 3
    call mul
    pop
    alloc 2
    je end
label end
    pop
    hlt

; A <- mul(A, B)
label mul
    ; A'
    load -3
label _mul_for
    ; IF B < 2
    load -3
    push 2
    jl _mul_end
    ; B <- B - 1
    load -3
    push 1
    sub
    store -4 -1
    pop
    ; A <- A + A'
    load -4
    load -2
    add
    store -5 -1
    pop
    alloc 2
    je _mul_for
label _mul_end
    pop
    ret
